# Scripts for www2.buecherhallen.de

## merkliste_verfuegbarkeit.user.js

When ` /startseite` is loaded, this script automatically fetches the list called "Merkliste".
For each entry on this list it fetches the details, mainly the availability at "Zentralbibliothek".
It then creates an overlay showing a table with all available items.